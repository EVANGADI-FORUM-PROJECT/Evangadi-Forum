/*
 * TASK: Frontend RAG Document Status
 *
 * TODO [T-24]: Render a status badge for processing/ready/failed states.
 */

export default function StatusBadge({ status }) {
  // TODO [T-24]: Implement status badge.
  return <span>{status}</span>;
}
